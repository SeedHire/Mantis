name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & Vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Vet
        run: go vet ./...

      - name: Build (linux/amd64)
        run: go build -o /tmp/mantis ./cmd/mantis

      - name: Build (darwin/arm64 cross-check)
        run: GOOS=darwin GOARCH=arm64 go build -o /dev/null ./cmd/mantis

      - name: Build (windows/amd64 cross-check)
        run: GOOS=windows GOARCH=amd64 go build -o /dev/null ./cmd/mantis

      - name: Run tests
        run: go test ./...

      - name: Smoke test — init + lint
        run: |
          /tmp/mantis init --lang ts
          /tmp/mantis lint || true   # lint may report violations; we just check it runs
